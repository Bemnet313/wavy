@@ -8,6 +8,7 @@
 import '../theme/app_theme.dart';
 import 'package:cached_network_image/cached_network_image.dart';
+import 'package:google_fonts/google_fonts.dart';

 class WavyCard extends StatelessWidget {
   final WavyItem item;
@@ -43,15 +44,15 @@
                       imageUrl: item.images.first,
                       fit: BoxFit.cover,
                       placeholder: (_, __) => Container(
-                        color: const Color(0xFFF3F0FF),
+                        color: WavyTheme.surfaceDark.withValues(alpha: 0.8),
                         child: const Center(
-                          child: CircularProgressIndicator(color: WavyTheme.primary),
+                          child: CircularProgressIndicator(color: WavyTheme.primaryLight),
                         ),
                       ),
                       errorWidget: (_, __, ___) => Container(
-                        color: const Color(0xFFF3F0FF),
-                        child: const Icon(Icons.image_outlined, size: 48),
+                        color: WavyTheme.surfaceDark.withValues(alpha: 0.8),
+                        child: Icon(Icons.image_outlined, size: 48, color: Colors.white.withValues(alpha: 0.2)),
                       ),
                     )
                   : Image.asset(
@@ -58,8 +59,8 @@
                       item.images.first,
                       fit: BoxFit.cover,
                       errorBuilder: (_, __, ___) => Container(
-                        color: const Color(0xFFF3F0FF),
-                        child: const Icon(Icons.image_outlined, size: 48),
+                        color: WavyTheme.surfaceDark.withValues(alpha: 0.8),
+                        child: Icon(Icons.image_outlined, size: 48, color: Colors.white.withValues(alpha: 0.2)),
                       ),
                     ),
@@ -82,10 +83,10 @@
                   Container(
                     padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 4),
                     decoration: BoxDecoration(
-                      color: WavyTheme.primary,
+                      color: WavyTheme.primaryDark,
                       borderRadius: BorderRadius.circular(12),
                     ),
                     child: Text(
                       item.condition,
-                      style: const TextStyle(
+                      style: GoogleFonts.spaceGrotesk(
                         color: Colors.white,
                         fontSize: 12,
@@ -107,3 +108,3 @@
                     child: Container(
-                      color: Colors.white.withOpacity(0.9),
+                      color: WavyTheme.surfaceDark.withOpacity(0.8),
                       padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 8),
@@ -118,10 +119,10 @@
                                   Text(
                                     locale == 'am' ? (item.titleAm ?? item.title) : item.title,
-                                    style: const TextStyle(
+                                    style: GoogleFonts.spaceGrotesk(
                                       fontSize: 18,
                                       fontWeight: FontWeight.w800,
-                                      color: WavyTheme.textPrimary,
-                                    ),
+                                      color: Colors.white,
+                                    ).copyWith(fontFamilyFallback: const ['Noto Sans Ethiopic']),
                                     maxLines: 1,
                                     overflow: TextOverflow.ellipsis,
@@ -130,8 +131,8 @@
                                   Text(
                                     '${item.price} ${item.currency}',
-                                    style: const TextStyle(
+                                    style: GoogleFonts.spaceGrotesk(
                                       fontSize: 16,
                                       fontWeight: FontWeight.w700,
-                                      color: WavyTheme.primary,
-                                    ),
+                                      color: WavyTheme.primaryLight,
+                                    ).copyWith(fontFamilyFallback: const ['Noto Sans Ethiopic']),
                                   ),
@@ -140,8 +141,8 @@
                               Text(
                                 'Size: ${item.size}',
-                                style: const TextStyle(
+                                style: GoogleFonts.spaceGrotesk(
                                   fontSize: 12,
                                   fontWeight: FontWeight.w600,
-                                  color: WavyTheme.textSecondary,
-                                ),
+                                  color: Colors.white.withValues(alpha: 0.6),
+                                ).copyWith(fontFamilyFallback: const ['Noto Sans Ethiopic']),
                               ),
